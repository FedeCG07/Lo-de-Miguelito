model Client {
  idClient        Int @id @default(autoincrement())
  fullName      String
  email String @unique
  phoneNumber Int
  password String
  address String
  totalOrders Int @default(0)
  role String @default("Client")

  orders Order[]
}
